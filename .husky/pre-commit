branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo "Direct commits to main are not allowed — branch off main first (see CLAUDE.md git workflow)."
  exit 1
fi

pnpm exec lint-staged
pnpm run typecheck
